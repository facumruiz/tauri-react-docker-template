version: "3"

services:
  tauri-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
    command: bash
